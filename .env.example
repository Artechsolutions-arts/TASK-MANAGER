# Copy this file to .env and fill in your values. Do not commit .env.

# MongoDB Atlas (use this to point Docker/backend to Atlas instead of local MongoDB)
DATABASE_URL=mongodb+srv://USER:PASSWORD@cluster.xxxxx.mongodb.net/appotime?retryWrites=true&w=majority

# Optional: override for docker-compose backend/celery (same as above when using Atlas)
# SECRET_KEY=your-secret-key-min-32-chars
